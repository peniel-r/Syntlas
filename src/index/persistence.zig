const std = @import("std");

// Placeholder for persistence module
// Will implement binary format, save/load, and versioning

pub const IndexPersistence = struct {
    pub fn init() IndexPersistence {
        return .{};
    }
};
